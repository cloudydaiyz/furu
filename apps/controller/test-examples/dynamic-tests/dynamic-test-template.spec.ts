// Template for a parsed dynamic test file

import { test } from '@playwright/test';

test('name-of-custom-test', async ({ page }) => {
  /**
   * Dynamic tests have global access to the `page` object to perform
   * Playwright specific functionality.
   */
  
  /* TEST CONTENT HERE */
});