// Template for a workflow parsed as a test file

import { test } from '@playwright/test';

test('workflow-title', async ({ page }) => {
  /**
   * Workflows have global access to the `page` object to perform
   * Playwright specific functionality.
   */

  /* WORKFLOW CONTENT HERE */
});